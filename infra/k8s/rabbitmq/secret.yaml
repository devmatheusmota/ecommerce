# In production, use sealed-secrets, external-secrets, or a secret manager.
# This is for local dev only — override in real environments.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secret
  namespace: ecommerce
  labels:
    app: rabbitmq
type: Opaque
stringData:
  username: ecommerce
  password: ecommerce_dev
